config.js

# зависимости (корень и вложенные пакеты)
node_modules/
**/node_modules/

# CRA / старый SPA (больше не используется; тема — закоммиченные public/static/css + media)

# Сборка полного React-демо ArchitectUI (npm run build:architectui-demo) — большой артефакт
public/architectui-react-pro/

# Исходники шаблона (кладите локально; не в public/)
vendor/architectui-react-pro/
public/html/

# IDE / локальные правила
.cursor/

.DS_Store
*.log
server.log
worker.log
