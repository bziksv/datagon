config.js

# зависимости (корень и вложенные пакеты)
node_modules/
**/node_modules/

# CRA / старый SPA (больше не используется; тема — закоммиченные public/static/css + media)

# IDE / локальные правила
.cursor/

.DS_Store
*.log
server.log
worker.log
