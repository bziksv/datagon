---
id: moysklad
title: Мой Склад (товары)
description: Синхронизация с API МС, таблица ms_export, фильтры, статусы, типичные сбои
---

Экран **`/moysklad.html`** — работа с таблицей **`ms_export`**: данные, подтянутые из **API МойСклад**, фильтры по типу номенклатуры, поиск по строкам экспорта, запуск и контроль **фоновой синхронизации**, вспомогательные действия (пересборка кэшей связей — если доступно в вашей сборке).

<blockquote class="dg-doc-tip">
<strong>Снимок интерфейса.</strong> PNG обновляют: <code>npm run docs:capture-screenshots</code> (с <code>DOCS_USER</code> и <code>DOCS_PASSWORD</code> — с живой панели; без входа — с макета <code>/doc-screenshots/moysklad-sample.html</code>) и <code>npm run docs:docusaurus:build</code>. Для этой таблицы — <strong>viewport</strong> (видимая область окна). <a href="./capture-screenshots.md">Подробнее о съёмке</a>.
</blockquote>

<figure class="dg-doc-shot">
<img src="/docs/screenshots/moysklad.png" alt="«МойСклад»" loading="lazy" />
<figcaption>МойСклад: фильтры, сводки и начало таблицы (кадр <strong>viewport</strong> — видимая область окна, не вся прокрутка).</figcaption>
</figure>

## С чего начать новому пользователю

1. Убедитесь, что в `config.js` или в переменной окружения **`MS_TOKEN`** задан рабочий токен с правами на чтение нужных сущностей МС.
2. Откройте **Мой Склад (товары)** и проверьте **статус** последней задачи (не висит ли ошибка с прошлого запуска).
3. При необходимости нажмите **Синхронизация** и дождитесь завершения (индикатор в UI + [Логи](/docs/processes/)).
4. Используйте **поиск** и фильтры, чтобы найти конкретный **код/артикул** и сверить поля с вашей учётной политикой.

## Блоки интерфейса

Страница состоит из **двух карточек**: «Фильтры и действия» и «Выгрузка МойСклад» (карточка с таблицей). Этот же макет — **эталон** для всех новых списочных страниц Datagon, см. правило `.cursor/rules/datagon-list-page-baseline-moysklad.mdc`.

### Карточка «Фильтры и действия»

| Блок | Назначение |
|------|------------|
| Шестерёнка `⚙` справа в заголовке | Открывает дропдаун **«Поля фильтров»** с переключателями: что показывать в форме. Поддерживает **auto-discovery** — новый input с атрибутом `data-dg-ms-filter-field="<key>"` появляется в списке автоматически, даже если разработчик забыл добавить ключ в группы (см. `datagon-table-filter-apply.mdc`, раздел «Авто-обнаружение новых полей»). |
| Кнопка `Свернуть/Развернуть` справа в заголовке | Прячет/показывает только тело карточки фильтров; сохраняется в `localStorage` (`datagon_moysklad_filters_collapsed_v1`). |
| Ряд 1 (форма) | **Умный поиск** (`code`, `name`, `supplier`, `supplier2`; ключи `sku:`, `name:`, `supplier:`, `manager:`, `stock:`; группы через `|`), `ID/КОД`, `Наименование товара`, `Тип`, `Архивные`, `Поставщик`, `Поставщик 2`. |
| Ряд 2 (форма) | `Менеджер`, `Контент-менеджер`, `Остаток от/до`, `Складская позиция`, `На сайте`, `На странице`. **Дубликатов** «табличный/API» вариантов одного и того же поля нет (раньше были `ms-tf-supplier` ↔ `ms-f-supplier` — устранено). |
| Чекбоксы | `Только с остатком > 0`, `Перестали сотрудничать`, `Только с закупочной ценой`. |
| Действия | `Применить` (primary), `По умолчанию` (outline) — далее: `Запустить синхронизацию`, `Остановить`, `Пересчитать остатки комплектов`, `Перестроить кэш связей`, `Обновить`, `Скопировать ссылку`. |
| Подсказка-параграф под кнопками | Видна только когда идёт фоновая задача или сервер прислал реальный `message`. В простое скрыта (никаких заглушек «Ожидание»). |

### Карточка «Выгрузка МойСклад» (таблица)

| Блок | Назначение |
|------|------------|
| Заголовок | Слева в `card-header`: иконка + «Выгрузка МойСклад». |
| Поле «Умный поиск» в шапке | Зеркалит основное поле фильтров (`#ms-f-search`): двусторонняя синхронизация, **Enter** — кликает `Применить`, **Esc** или нативный «×» — очищает оба поля и сразу применяет. На каждое нажатие клавиш серверных запросов нет (правило `datagon-table-filter-apply.mdc`). |
| Кнопка `🧩 Столбцы` | Открывает панель чекбоксов видимости столбцов (источник — массив `MS_COLS`). |
| Кнопка `📏 Ширины` | Ручная ширина каждой колонки в пикселях (тоже из `MS_COLS`); альтернатива — drag за правый край заголовка. |
| Кнопка `Свернуть/Развернуть` | Прячет тело карточки таблицы (заголовок с поиском и кнопками остаётся виден). |
| Таблица | Строки `ms_export`; широкая таблица с горизонтальным скроллом и `sticky`-th под верхним меню. |
| Пагинация | `На странице` / `Найдено: N; страница X/Y; показано: K` / кнопки `Назад / Вперёд` (по 100 строк по умолчанию). |

### Сводка над таблицей

| Карточка | Что считает |
|----------|-------------|
| **Всего в выборке** | Количество строк в текущем фильтре. |
| **Товары / Комплекты** | Раздельно по типу. |
| **Сумма по товарам** | `остаток × закупочная цена` только по строкам `Товар` (`GET /api/ms/stats → inventory_value_products`). |
| **Сумма по комплектам** | То же, но по `Комплект` (`inventory_value_bundles`). Не смешивается с суммой по товарам, чтобы один комплект и его компоненты не попадали в одну цифру. |

## Типовой сценарий сверки с «Моими товарами»

В [Мои товары](/docs/myproducts/) признак **связи с МС** опирается на сопоставление **нормализованных** кодов между `my_products` и `ms_export`. Если связь «плавает»:

- выполните повторный **sync** МС;
- проверьте, что в МС и у вас в источнике **одинаковая логика** артикула (пробелы, регистр нивелируются на сервере — см. API);
- при массовых правках — кэш статистики может задержаться на TTL.

## Если синхронизация не идёт

1. [Логи](/docs/processes/) — не застряла ли предыдущая задача.
2. Токен и лимиты API МС (ошибки 401/403 в логах сервера).
3. `GET /api/ms/status` — вручную через API (см. [документацию API](/docs/api/#moysklad)).
4. После смены токена в конфиге — **перезапуск Node**.

## Колонки `ms_export` (общий смысл)

Набор колонок зависит от версии выгрузки; ориентиры:

| Тема | Что смотреть |
|------|----------------|
| **Код / артикул** | Связь с [Мои товары](/docs/myproducts/) после нормализации. |
| **Тип** | Товар / **комплект** — комплекты часто требуют отдельной бизнес-логики в отчётах. |
| **Остаток, цены** | Сверка с витриной МС; расхождения — часовой пояс и налоги. |
| **«На сайте» / архив** | Фильтры в UI; помогают отсечь неактуальные строки при ручной проверке. |

## Лимиты API МойСклад

У МС есть **rate limit** и размер страницы; в Datagon см. `ms_sync_page_limit` и `ms_sync_delay_ms` в [Настройках](/docs/settings/). При `429` или таймаутах — **увеличьте** паузу, **уменьшите** страницу, не запускайте второй синк, пока первый не завершён.

### Комплекты: состав в кэше и пересчёт остатков

Загрузка списка **`/entity/bundle`** с `expand=components,...` в API МойСклад **нестабильна при большом `limit`** (часто в ответе остаётся только `meta` без `components.rows`). Поэтому при синхронизации список комплектов запрашивается **пакетами не больше 100 строк** на страницу (независимо от `ms_sync_page_limit` для товаров), чтобы в **`ms_entity_details`** попали полные карточки с составом.

Если после обновления Datagon вы всё ещё видите в итоге пересчёта «пропущено: N» почти для всех комплектов — выполните **полный sync** МойСклад ещё раз: старые записи в `ms_entity_details` могли быть сохранены без строк состава. Кнопка «Пересчитать остатки комплектов» при расчёте подтягивает код номенклатуры по `href` компонента (как при полном синке), для этого нужен рабочий **`MS_TOKEN`**.

## API (для автоматизации)

- `POST /api/ms/sync` — старт фона.
- `GET /api/ms/status` — состояние.
- `GET /api/ms/export` — страницы таблицы с query `search`, `type`, `limit`, `offset`.

После успешного сохранения **`ms_export`** (в конце полного синка) выполняется пакетное обновление **`dg_product_zero_stock_log`** за сегодня: только строки со складской позицией «Да», не в архиве, и при этом **остаток ≤ 0** или (для кода **без** дефиса в номенклатуре) **остаток строго меньше минимального числового суффикса** среди кодов вида `<тот же код>-<целое>` в той же выгрузке (например `27877-2` и `27877-10` → порог 2 шт. для базы `27877`). **Импорт отгрузок** («Продажи МС», `entity/demand`) к этому шагу **не относится**. Затем тем же событием заполняется **`dg_product_stock_snapshot`**: по каждому коду в `ms_export` — одна строка на текущую календарную дату с полем `stock` (повторный синк в тот же день перезаписывает значение; данные старше **`product_stock_snapshot_retention_days`** из настроек Datagon удаляются при синке, по умолчанию **365** дней).

Полное описание — [REST API — MoySklad](/docs/api/#moysklad).

## Эталон списочной страницы

`/moysklad.html` — **эталон** для всех новых списочных страниц Datagon vanilla. Любая новая страница со списком/таблицей делается **по аналогии** с этой страницей; контракт зафиксирован в правилах:

- `.cursor/rules/datagon-list-page-baseline-moysklad.mdc` — структура двух карточек, кнопки в card-header, шестерёнка с auto-discovery, поиск-зеркало в шапке, единый источник `<X>_COLS` для столбцов/ширин/`<thead>`/`<tbody>`, чек-лист для новой страницы.
- `.cursor/rules/datagon-table-behavior-lock.mdc` — sticky-th, scroll, borders, без разрыва `thead`/`tbody`, пагинация по 100, формат счётчика «Найдено: N; страница X/Y; показано: K».
- `.cursor/rules/datagon-table-filter-apply.mdc` — двухслойная модель состояния (`pending` ↔ applied), применение фильтров **только** по кнопке «Применить» или Enter в поле поиска, auto-discovery новых полей в шестерёнке.
- `.cursor/rules/datagon-ui-baseline-master-lock.mdc` — мастер-лок, перечисляет все правила.

Файлы-референсы (читать при сомнениях):

- `static-html/vanilla/inners/moysklad.inner.html` — разметка двух карточек.
- `static-html/vanilla/inners/moysklad.scripts.html` — IIFE: `filtersCollapse`, `tableCardCollapse`, `columnControls`, `bindHeaderSearchMirror`, `moyskladFilterFieldVisibility`, `renderStatusHint`.
- `static-html/vanilla/inners/moysklad.head.html` — CSS: `.datagon-columns-toolbox`, `.datagon-columns-panel`, `.dg-ms-header-search`, sticky-`th` для wide-таблицы.
- `static-html/vanilla/datagon-vanilla.js` — общая утилита `DatagonFilterFieldsVisibility.init` с auto-discovery.

### Текущий статус приведения к эталону по страницам

Краткая матрица соответствия эталону (на момент последней правки). «Полное» — все ключевые элементы карточек присутствуют; «частичное» — ключевые элементы есть, но есть отдельные отклонения; «отдельный профиль» — у страницы своя архитектура, baseline применим только частично.

| Страница | Статус | Что сделано / что отличается |
|----------|--------|------------------------------|
| `/moysklad.html` | **Эталон** | Источник правды. |
| `/results.html` | **Полное** | Шестерёнка `Поля фильтров` в card-header «Фильтры и действия». В `card-header` таблицы: **поиск-зеркало** `#dg-res-table-search-mirror` (sync с `#dg-res-f-search`, Enter/Esc), прокси-кнопки **`🧩 Столбцы`** `#dg-res-columns-toggle-header` и **`📏 Ширины`** `#dg-res-widths-toggle-header` (клик → оригиналы `#dg-res-columns-toggle` / `#dg-res-widths-toggle` в скрытом `.columns-top` внутри toolbox; обёртка `#dg-res-toolbox-panels` управляется `syncWrap` по классу `d-none`, открытие одной панели закрывает другую, клик вне обеих — закрывает обе), `Свернуть/Развернуть`. Ширины индексируются по колонке (`applyColumnWidthByIndex`, диапазон 80–900 px), persisted в `localStorage` под `userColWidthKey()`; кнопка `Ширины по умолчанию` `#dg-res-widths-default` сбрасывает все ширины через `clearResColumnSizing(idx)` для каждой колонки. |
| `/queue.html` | **Полное** | Шестерёнка, toggle фильтров, **поиск-зеркало** `#dg-q-table-search-mirror` в card-header, **`🧩 Столбцы`** (`#dg-q-columns-toggle`), **`📏 Ширины`** (`#dg-q-widths-toggle` + панель `#dg-q-widths-panel`, кнопка `Ширины по умолчанию` `#dg-q-widths-default`, список `#dg-q-widths-list`) и **`Свернуть`** в card-header таблицы; обёртка `#dg-q-toolbox-panels`. Ширины по индексу колонки (80–420 px, как в `applyColumnWidthByIndex` в `queue.scripts.html`); та же связка с drag-резайзером на заголовке и `columnWidths` в `localStorage`. |
| `/projects.html` | **Полное** | Поиск-зеркало `#dg-proj-table-search-mirror`, `🧩 Столбцы` (`#dg-proj-columns-toggle`), **`📏 Ширины`** (`#dg-proj-widths-toggle` + панель `#dg-proj-widths-panel` с `Ширины по умолчанию` `#dg-proj-widths-default` и списком `#dg-proj-widths-list`) и `Свернуть` в шапке таблицы. Ширины индексируются по порядку колонки (см. `applyColumnWidthByIndex` в `projects.scripts.html`); используется та же связка с drag-резайзером на правом крае `<th>` и persisted-объектом `columnWidths` (`localStorage` ключ `userColWidthKey()`). |
| `/my-products.html` | **Полное** | Поиск-зеркало `#dg-mp-table-search-mirror`, `🧩 Столбцы` (`#dg-mp-columns-toggle`), **`📏 Ширины`** (`#dg-mp-widths-toggle` + панель `#dg-mp-widths-panel` с `Ширины по умолчанию` `#dg-mp-widths-default` и списком `#dg-mp-widths-list`) и `Свернуть` в шапке таблицы; обёртка `#dg-mp-toolbox-panels` с `d-none`. Ширины индексируются по порядку колонки (см. `applyColumnWidthByIndex`); используется та же связка с drag-резайзером на правом крае `<th>` и persisted-объектом `columnWidths` (`localStorage` ключ `userColWidthKey()`). |
| `/my-sites.html` | **Полное** | Три карточки со `Свернуть` **в card-header**: «Добавить мой сайт» (`#ms-form-collapse` → `#dg-msites-add-body`, persist `mysites_add_form_collapsed_v1`; «Изменить» разворачивает форму), «Фильтры и действия» (шестерёнка + `Свернуть`), «Список сайтов» (поиск-зеркало `#dg-msites-table-search-mirror` + `Свернуть`). _Осознанное отклонение_: `🧩 Столбцы` отсутствует — у таблицы 4 фиксированных колонки (id/название/домен/статистика), управление видимостью избыточно. |
| `/matches.html` | **Полное (шаг 1 + шаг 2)** | Дубликат лейбла **«Лимит»** разведён: `«Лимит списка»` / `«Лимит очереди»`. **Карточка «Мои товары для ручного запуска»:** в `card-header` рядом с `Свернуть` — **`📏 Ширины`** `#dg-matches-my-widths-toggle`, панель `#dg-matches-my-widths-panel` (7 колонок: выбор / ID·КОД / SKU / название / цена / валюта / обновлён), кнопка `Ширины по умолчанию` `#dg-matches-my-widths-default`, обёртка `#dg-matches-my-toolbox-panels` с `d-none`. Ширины — `applyMatchesMyColumnWidth`, persisted `userMatchesMyColWidthKey()`, drag на `<th>` (`initMatchesMyColumnResizeHandles`). _Зеркала умного поиска для этой таблицы нет_ (данные с сервера по сайту запуска). **Карточка «Совпадения»:** поиск-зеркало `#dg-matches-table-search-mirror`, **`📏 Ширины`** `#dg-matches-widths-toggle` + `#dg-matches-widths-panel` (6 колонок), `#dg-matches-toolbox-panels`. Шестерёнка «Поля фильтров», `applyMatchesColumnWidth` / `userMatchesColWidthKey()`. _Осознанное отклонение_: `🧩 Столбцы` нет — композитные колонки совпадений фиксированы по смыслу. |
| `/exports-marketplaces-ozon.html`, `wildberries`, `yandex` | **Полное** | Над «Выгрузка <Shop>» через assemble подключается общий fragment `inners/exports-marketplaces-shop-filters.fragment.html` — карточка **«Фильтры и действия»** (шестерёнка `#dg-mp-shop-filter-blocks-btn` с auto-discovery, `Свернуть/Развернуть` `#dg-mp-shop-filters-toggle`, primary-поле `#dg-mp-shop-smart`, кнопки `Применить` / `По умолчанию`). В `card-header` таблицы: левая группа (заголовок + mirror `#dg-mp-search`, ограниченная ширина), справа прокси-кнопки `🧩 Столбцы` / `📏 Ширины` (клик прокидывается на оригинальные в toolbox внутри `card-body` — выпадающие панели не перепозиционируются) + `Свернуть`. Дубликат лейбла «По умолчанию» в toolbox разведён на «Столбцы по умолчанию» и «Ширины по умолчанию». Расширенные фильтры (поставщики, менеджеры) сохранены в toolbox. **Не трогать** sticky-разметку (`.dg-mp-mkt-scroll`, `overflow: visible !important`, `--dg-table-sticky-top`) — см. `datagon-table-behavior-lock.mdc`. |
| `/exports-marketplaces-issues.html` | **Полное** | Под заголовком страницы — карточка **«Гайд»** (`dg-mp-issues-workflow-guide`, переключатель `#dg-mp-issues-workflow-toggle`, три сворачиваемых шага с persist в `datagon_mp_issues_workflow_*`), по образцу `/matches.html`. Карточка «Фильтр маркетплейса» переименована в **«Фильтры и действия»** + добавлена шестерёнка `#dg-mpu-filter-blocks-btn` (auto-discovery, через `data-dg-mpu-filter-field` на блоках scope/manager/cmgr/filter_actions — четыре поля). **Все фильтры теперь живут в одной карточке**: радио-сцепка `Фильтр маркетплейса` (`Все товары / Есть проблемы / Нет ни на одном / Нет на Ozon|WB|ЯМ`), селекты `Менеджер` (`#dg-mpu-flt-manager`) и `Контент-менеджер` (`#dg-mpu-flt-cmgr`), а также блок действий `Применить` (`#dg-mpu-flt-apply`) / `По умолчанию` (`#dg-mpu-flt-reset`) с бейджем «не применено» (`#dg-mpu-flt-dirty`) — раньше последние три блока висели в toolbox таблицы. В обеих карточках кнопка `Свернуть` в card-header. В `card-header` карточки таблицы — **только** умный поиск `#dg-mp-search` в виде эталонного `input-group` с иконкой 🔎 и тултипом (Enter — применить, Esc — очистить через нативный `<input type="search">`); кнопка `#dg-mp-search-clear` оставлена скрытой `d-none` ради обратной совместимости с обработчиком в `inners/exports-marketplaces-issues.scripts.html`. Прокси-кнопки `🧩 Столбцы` / `📏 Ширины` в card-header — клик на скрытые `#dg-mpu-cols-toggle` / `#dg-mpu-widths-toggle` в `.dg-mp-toolbox-top`; панели `#dg-mpu-cols-panel` / `#dg-mpu-widths-panel` — полноширинные `.dg-mp-toolbox-panel` в `#dg-mpu-toolbox`, как у shop-маркетплейсов (сетка `.dg-mp-cols-grid` / `.dg-mp-widths-grid`, кнопки «Столбцы по умолчанию» / «Ширины по умолчанию» в `.dg-mp-toolbox-actions`). **Wide-таблица:** эталон — `/my-products.html` (`#dg-mpu-table-scroll-outer`, плавающий `thead#dg-mpu-thead` через JS), не схема shop (`.dg-mp-mkt-scroll` + native sticky к viewport). См. `.cursor/rules/datagon-table-behavior-lock.mdc` (раздел «Проблемы с товарами»). |
| `/exports-huckster.html` | **Полное (admin-профиль)** | Шестерёнка **«Поля фильтров»** `#dg-huckster-filter-blocks-btn` в card-header «Обновление Huckster» с auto-discovery по `data-dg-huckster-filter-field` (manager/ozon/wb/yandex/models + кнопка reset, переименована в **«По умолчанию»** `#dg-huckster-filter-reset`). Каждая из трёх matrix-карточек имеет собственный smart-search в card-header (`#dg-huckster-smart-search-1`, `-2`; «Потеряшки» — без поиска, по сценарию маленький подсписок), `Свернуть/Развернуть` (`-export-toggle`, `-rrc-toggle`, `-lost-toggle`) и эталонную кнопку **`📏 Ширины`** (`#dg-huckster-widths-toggle-1` / `-2` / `-lost`) рядом с `Свернуть` (обёртка `.dg-huckster-matrix-actions`, чтобы не ломать grid-area `toggle` в toolbar). Полоса `colwidth-strip` теперь живёт в `card-body` под `id="dg-huckster-widths-panel-1|2|lost"` и **скрыта по умолчанию** (`d-none`); раскрывается кликом по `📏 Ширины`, состояние per-user сохраняется в `localStorage` под `datagon_huckster_matrix_widths_panel_open_v1.<which>.<user>`. _Осознанные отклонения_: `🧩 Столбцы` отсутствует — у matrix фиксированный набор кабинетов (UID + Наименование + Ozon/WB/ЯМ-блоки), управление видимостью требовало бы отдельной фичи; в карточке «Обновление Huckster» фильтры применяются live (без двухслойного pending/applied) — это admin-сценарий точечной правки фильтров, переход на «Применить» требует переписать всю обвязку матрицы (см. «Не найдено», `Исключить архивные комплекты` и т.п.). |

Дубликаты фильтров (одинаковые лейблы в одной форме) **не найдены** ни на одной из проверенных страниц. Единственное двусмысленное совпадение — две кнопки «По умолчанию» в маркетплейс-toolbox — устранено переименованием.
