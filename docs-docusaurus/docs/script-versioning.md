---
id: script-versioning
title: Версионирование скриптов синка
description: Обязательные ревизии при изменении алгоритмов синка, матриц и фоновых задач Datagon
---

При изменении **алгоритма** синка, сборки матрицы или фоновой задачи нужно **поднимать версию** в том же PR / той же сессии, что и правка кода. Иначе нельзя отличить снапшот, собранный старым скриптом, от нового.

## Зачем

- в БД лежит `latest`-снапшот без привязки к git-коммиту;
- поддержка и интеграторы (Huckster и др.) спрашивают «какой скрипт отдал эти строки»;
- багфиксы пагинации/фильтров меняют состав данных — это должно быть видно в UI и API.

## Эталон: Huckster

| Слой | Файл / место |
|------|----------------|
| Реестр ревизий | `lib/hucksterSyncRevision.js` |
| Алгоритм | `routes/exportsHuckster.js`, `lib/hucksterMsBridgeMatrix.js`, `lib/hucksterSnapshotStore.js` |
| API | поле `sync_script` в `GET /api/exports/huckster/config`, `GET …/sync-status`, `GET …/snapshot`, в `result` после sync |
| UI | бейдж `v1.0.1 · rev.2` на `/exports-huckster.html` |
| Справка API | раздел [Exports / Huckster](./api.md#exports--huckster) |

Пример записи ревизии:

```javascript
{
  revision: 2,
  version: '1.0.1',
  date: '2026-05-26',
  matrix_kind: 'ms_bridge_v1',
  notes: 'Unit/repricer: offset += длина страницы; …',
}
```

## Когда поднимать версию

**Обязательно:**

- логика пагинации, фильтров, маппинга полей;
- состав или смысл колонок матрицы;
- порядок фаз синка (repricer → unit → мост МС и т.д.);
- багфикс, из‑за которого в выборку попадают/пропадают строки.

**Не обязательно:**

- рефакторинг без изменения результата;
- правки стилей/текстов UI без смены алгоритма;
- только документация.

## Semver (практика)

| Изменение | `version` | `matrix_kind` |
|-----------|-----------|---------------|
| Багфикс, тот же формат матрицы | patch (`1.0.0` → `1.0.1`) | без смены |
| Новое поведение, старые снапшоты читаются | minor (`1.0.1` → `1.1.0`) | без смены или новый kind с back-compat в UI |
| Другая схема JSON / колонок | major или новый kind | **новое** значение, новая ревизия |

`revision` — монотонный счётчик строк в реестре (1, 2, 3…).

## Контракт для нового модуля синка

1. Создать `lib/<name>SyncRevision.js` по образцу Huckster.
2. Экспортировать `get<Name>SyncMeta()` и использовать **только** его для версии в рантайме.
3. Добавить поле метаданных в API статуса/конфига/снапшота.
4. Писать мету в payload при сохранении в БД.
5. Показать версию на экране панели (бейдж из API).
6. Описать поле в `api.md` и дописать строку в историю на этой странице.

## История ревизий Huckster (кратко)

| rev | version | date | notes |
|-----|---------|------|-------|
| 1 | 1.0.0 | 2026-05-26 | Repricer/unit limit=900; мост ms_export; набор 1 — фильтр Unit «онлайн»+«калькулятор». |
| 2 | 1.0.1 | 2026-05-26 | Пагинация unit/repricer: `offset +=` длина страницы; короткая страница не конец, если `cursor.total > offset` (3110 / модель ЯМ). |
| 3 | 1.0.2 | 2026-06-02 | Unit-модели ЯМ: shop timeout 300s (было 90s) после полной пагинации. |
| 4 | 1.0.3 | 2026-08-18 | Сшивка кода МС ↔ UID Huckster без учёта регистра. |
| 5 | 1.0.4 | 2026-08-18 | Unit ЯМ: timeout страницы 180s / shop 600s; сбой одного set не обнуляет кабинет. |

Полный список — в `lib/hucksterSyncRevision.js`.

## Cursor (для разработчиков)

Правило агента: `.cursor/rules/datagon-script-version-revision.mdc` (always apply).
