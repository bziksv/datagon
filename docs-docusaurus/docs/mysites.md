---
id: mysites
title: Мои сайты
description: Подробно — подключение БД Bitrix/Webasyst, поля, синхронизация my_products, ошибки
---

Раздел **`/my-sites.html`** — учёт **источников** собственного каталога: подключение к **внешней MySQL** (магазин на Bitrix, Webasyst и т.д.), проверка чтения полей и **синхронизация** данных в локальную таблицу Datagon **`my_products`**. Без рабочих источников неполноценны «Мои товары», сопоставление и часть отчётов по конкурентам.

<blockquote class="dg-doc-tip">
<strong>Снимок интерфейса.</strong> PNG обновляют: <code>npm run docs:capture-screenshots</code> (с <code>DOCS_USER</code> и <code>DOCS_PASSWORD</code> — с живой панели; без входа — с макета <code>/doc-screenshots/mysites-sample.html</code>) и <code>npm run docs:docusaurus:build</code>. Для этого экрана — полная страница в кадре (см. <a href="./capture-screenshots.md">съёмка</a>).
</blockquote>

<figure class="dg-doc-shot">
<img src="/docs/screenshots/mysites.png" alt="Раздел «Мои сайты»" loading="lazy" />
<figcaption>«Мои сайты»: заголовок, фильтры и таблица (полная страница в кадре; без входа в съёмке — макет).</figcaption>
</figure>

## Перед началом

Убедитесь, что:

- с сервера Datagon (или вашей машины, если так настроено) есть **сетевой доступ** к хосту БД источника (firewall, VPN, `bind-address` MySQL);
- у пользователя БД есть **SELECT** (и при необходимости другие права) на таблицу товаров и связанные таблицы (SKU для Webasyst);
- вы знаете **точное имя таблицы** и **имена колонок** для SKU, названия, цены, остатка, признака активности.

## Пошагово: новый источник

1. Откройте **Мои сайты** → кнопка добавления / форма нового подключения (зависит от версии UI).
2. Заполните **хост**, **порт** (часто `3306`), **имя базы**, **пользователь**, **пароль**.
3. Укажите **таблицу товаров** и сопоставьте поля (SKU, название, цена, валюта, остаток и т.д.) — используйте подсказки на форме.
4. Для **Webasyst**: отдельно укажите таблицу **SKU** и поля `wa_*`, если это требуется вашей схемой.
5. Нажмите **сохранить** / **проверить подключение** (если есть). При ошибке читайте текст: «DB Connect», «Access denied», «Unknown database».
6. Запустите **превью** или тестовую выборку (если доступно) — убедитесь, что в превью осмысленные строки.
7. Запустите **синхронизацию** (полную или поэтапную — см. подсказки). Длительные прогоны смотрите в [Логи](/docs/processes/).

## Что происходит при синхронизации (логика)

Перед полным прогоном часто выполняется подготовка: позиции помечаются **неактивными**, затем найденные во внешней БД **обновляются** и снова помечаются активными. Позиции, которых **нет** в источнике после прохода, могут остаться **неактивными** — это ожидаемо (см. FAQ на [главной](/docs/) про «неактивны после синка»).

## Типовые ошибки

| Симптом | Что проверить |
|---------|----------------|
| DB Connect / timeout | сеть, порт, хост, что MySQL слушает внешние подключения |
| Access denied | логин/пароль, хост пользователя (`user@'%'`) |
| Unknown table / column | имя таблицы и маппинг полей |
| Пустое превью | фильтр в запросе, пустая таблица, неверная схема |

## Bitrix и Webasyst: что чаще всего мапят

| Назначение | Bitrix (ориентир) | Webasyst (ориентир) |
|------------|-------------------|----------------------|
| Таблица товаров | часто `b_catalog_product` + `b_catalog_price` / поля в форме | своя схема `shop_product` и SKU-таблица |
| SKU / артикул | `ARTICLE`, `XML_ID` — зависит от настройки магазина | поля `wa_*` в форме |
| Цена / валюта | из каталога / типа цены | из SKU или товара — см. подсказки в UI |
| Остаток | `QUANTITY`, складские модули | `count` или кастомное поле |

Точные имена колонок снимите запросом `SHOW COLUMNS` / ER-диаграммой — не полагайтесь на чужие примеры из интернета.

## Безопасность и эксплуатация

- Пользователь БД для чтения лучше иметь **только SELECT** на нужные таблицы и **без** FILE/SHUTDOWN.
- **Пароль** источника хранится в БД Datagon — ограничьте доступ к бэкапам и к админам приложения.
- Долгий синк на «боевом» источнике — планируйте на низкую нагрузку; тяжёлые JOIN в настройках источника увеличивают блокировки на стороне магазина.

## Связь с другими разделами

- [Мои товары](/docs/myproducts/) — просмотр и действия по строкам `my_products`.
- [Сопоставление](/docs/matches/) — нужны актуальные «наши» SKU.
- [REST API — My sites](/docs/api/#my-sites) — `GET/POST/PUT/DELETE /api/my-sites`, `…/sync`, `sync-all-real`.
